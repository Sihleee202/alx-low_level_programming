#include "main.h"

/***
 * nul - multiplies two integers
 * @c: The first number to multiply with the first number
 * @y: The second number to multiply with first number
 * Return: The results of multipication between x param and y param
 */

int nul(int x, int y)

{

	return (x * y);

}
